---
- name: clone repo
  git:
    repo: "{{ repo_to_build }}"
    dest: "{{ where_repo_should_be }}"
    update: no
- name: Build an image
  docker_image:
    build:
      path: "{{ path_to_project }}"
    name: "{{ full_image_name }}"
    tag: "{{ tag }}"
    source: build
    force_source: yes